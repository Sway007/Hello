
* Implemented a custom neural network in NeuralNetwork.py. Prodiction performance is showed in console after every epoching.  

* Not sure whether there are some bug or the neural network created suitable to the classifaction problem, the accuracy is a little confusing, approximating to 0.1(1000 / 10000)